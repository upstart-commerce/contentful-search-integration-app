import { css } from 'emotion'

export const styles = {
  noResult: css({
    'overflow-y': 'auto',
    height: '100%',
    display: 'flex',
    'align-items': 'center',
    'justify-content': 'center',
    margin: '0',
  }),
}
