import { css } from 'emotion'

export const styles = {
  noResult: css({
    overflowY: 'auto',
    height: '100%',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    margin: '0',
  }),
}
